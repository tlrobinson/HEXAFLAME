{
  "name": "web-control",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8000",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 8000"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
